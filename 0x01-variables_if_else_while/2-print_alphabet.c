#include <stdio.h>
/**
 * Main - prints all alphabets on a new line
 *
 * Return: 0 (success)
 */
int main(void)
{
int i;
char alphabet[] = "abcdefghijklmnopqrstuvwxyz";

for (i = 0; i < alphabet[i] != '\0'; i++)
{
putchar(alphabet[i]);
putchat('\n');
}
return (0);
}
